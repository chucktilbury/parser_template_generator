// begin group function
case {{state_number}}:
if(expect_token(OPAREN)) {
    consume_token();
    {{increment_state_number}}
    state = {{state_number}};
}
else
    state = STATE_NO_MATCH;
break;

{{handle_group_items}}

case {{state_number}}:
if(expect_token(CPAREN)) {
    consume_token();
    {{increment_state_number}}
    state = {{state_number}};
}
else
    state = STATE_ERROR;
break;
// end group function
